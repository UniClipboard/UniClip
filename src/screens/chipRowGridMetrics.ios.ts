import { FILTER_CHIP_ROW_HEIGHT } from '@/components/HomeFilterChipsRow.types';
import type { ChipRowGridMetrics } from './chipRowGridMetrics.types';

export const CHIP_ROW_GRID_METRICS: ChipRowGridMetrics = {
  paddingTopExtra: 0,
  contentInsetTop: FILTER_CHIP_ROW_HEIGHT,
  progressViewOffset: 0,
};
